// words-excluder.tsで除外する単語
export const EXCLUDED_WORDS = ['NewsPicks'];

// rss-parserのOutput(Feed), Item型で明示的に取得する要素名
export const FEED_CUSTOM_FIELDS = ['lastBuildDate', 'docs', 'generator'];
export const ITEM_CUSTOM_FIELDS = ['description'];
